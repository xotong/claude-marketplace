# Auto-generated by static-appsec-scan skill
# Language: javascript | Source path: .

stages:
  - scans

include:
  - component: $CI_SERVER_FQDN/pipeline-component/secret-detection/secret-detection@main
    inputs:
      stage: scans

  - component: $CI_SERVER_FQDN/pipeline-component/container-scanning/container-scanning@main
    inputs:
      stage: scans
      cs_image: customdocker.artifactory.<YOUR_DOMAIN>/flatris:<TAG>

  - component: $CI_SERVER_FQDN/pipeline-component/fortify-sast/fortify-sast@main
    inputs:
      job-name: fortify-sast
      stage: scans
      upload-srm-option: "false"
      language: javascript
      source-path: .
      registry: customdocker.artifactory.<YOUR_DOMAIN>/
